- [x] add search result count feedback, shows "X results found" or "No results found" or "Request failed" or "Searching..."
- [x] add "jump to target terminal" button to each search result, if the result uuid matches one of the current running terminal uuid, otherwise grayout the button. generate terminal uuid for each running terminal.
- [ ] make session managing configurable: either use client side terminal capture, or use gnu screen or tmux as terminal session manager, so we can take image screenshots, extract text for each terminal session, without using frontend. configure if we use gnu screen or tmux in config file. also check if each binary is in path, if not in then prompt user to install and add to path. only if config file configured mode is check at the moment. make sure you use non-default server socket when launching gnu screen or tmux. if using tmux, unset TMUX environment variable, and create several environment variable settings to make sure nested tmux session works. related file: reference/terminal_screenshot_taking/tmux_kiosk.conf
- [ ] create example config yaml file in this directory named config.example.yaml
- [ ] take screenshot png for each terminal, store as file png in backend
- [ ] check out directory reference/flashback-screenshot-webui for reference, create or add webpage to get each historical terminal session detail, remember to take "terminal screenshot" or image in the backend, terminal text as "OCR" result, and store them in database, create timeline webpage, follow the style, make consistent look.